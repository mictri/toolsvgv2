# Tasks 28/07/2026

## 1. Export HTML/JS — cấu trúc chuẩn
- **gsapCompiler.ts**: morphSVG→string; ease không double-quote; `getSelector` phân loại group vs path; auto không dùng ScrollTrigger; hover/click → `paused: true`; scroll → ScrollTrigger config `markers: true`
- **htmlExporter.ts**: bỏ DOCTYPE/XML; bọc SVG trong `<div id="animation-svg" class="wrapper">`; DOMContentLoaded wrapper; 3 CDN gsap 3.15.0 (GSAP + ScrollTrigger + MorphSVGPlugin); 2 layout (centered / scroll với spacer 100vh)
- **ExportModal.tsx**: sửa `scrollStart` mặc định `'top 80%'`, type ExportOptions

## 2. Fix [object Object] bug
- Thêm hàm `toJsValue` xử lý object đệ quy → không còn `[object Object]` trong ScrollTrigger config

## 3. Xoá duplicate event handlers
- `gsapCompiler.ts` chỉ sinh timeline code thuần (gsap.to / gsap.fromTo), không kèm event listener
- `htmlExporter.ts` là nơi duy nhất sinh DOM event handlers (hover/click)

## 4. Save / Open project (.prosvg)
- `projectSerializer.ts`: dùng File System Access API (`showSaveFilePicker` / `showOpenFilePicker`) + fallback `<a download>`
- Lưu fabricObjects JSON + store state (layers, animatedObjects, duration, canvasConfig)
- App.tsx: handleSaveProject / handleOpenProject

## 5. Save As
- App.tsx: handleSaveProjectAs — luôn gọi file picker (không prompt tên)
- Gọi `saveProject(canvas, name, true)` để force picker

## 6. Layers Panel — phân cấp <g> + kéo thả
- `editorStore.ts`:
  - `reorderFolder`: move folder + toàn bộ con cháu trong flat array
  - `findDescendantIds`: collect tất cả descendant ids (0 → hết)
  - `moveLayerInParent`: sắp xếp trong cùng parentId
- `LayersPanel.tsx`:
  - Render tree `<g>` → folder, leaf → child item
  - Drag-drop dùng `dataTransfer`, cho phép reorder item cùng parentId (root, sub-folder, leaf)
  - `syncCanvasOrder` đọc leaf từ `layers.filter(l => l.parentId === folder.id)` (không dùng folder.childrenIds)
  - `collectLeafIds` dùng filter theo parentId để phản ánh thứ tự kéo thả

## 7. SVG Import — giữ nguyên toạ độ gốc
- **svgParser.ts**:
  - Gắn `data-fcv-idx` lên từng element SVG trước parse
  - Dùng `idxToFabricObject` map element→object qua index, không dùng element reference
  - Xoá `originX: 'center'`, `originY: 'center'` — objects giữ origin mặc định top-left
  - Xoá bounding box + offset centering — objects giữ nguyên toạ độ SVG gốc
- **App.tsx** `handleImportSvg`:
  - Lần import đầu: set canvas dimensions + canvasConfig từ svgWidth/svgHeight (parse từ viewBox/width/height)
  - Export SVG dùng canvasConfig dimensions

## 8. Export — tôn trọng Dimensions sidebar
- **App.tsx** `handleExportSvg`: truyền `canvasConfig.width/height` vào `serializeCanvas`
- **ExportModal.tsx**: import `useEditorStore` → lấy canvasConfig → truyền width/height vào cả `serializeCanvas` và `ExportOptions` (HTML max-width)

## Files modified
| File | Changes |
|------|---------|
| `src/services/svgParser.ts` | Xoá centering, thêm index matching |
| `src/App.tsx` | Import set canvas size, export dùng canvasConfig |
| `src/store/editorStore.ts` | Thêm reorderFolder, findDescendantIds, moveLayerInParent |
| `src/editor/layers/LayersPanel.tsx` | Tree render + drag-drop reorder |
| `src/editor/export/gsapCompiler.ts` | morphSVG string, ease, getSelector, toJsValue |
| `src/editor/export/htmlExporter.ts` | Cấu trúc chuẩn, DOMContentLoaded, 3 CDN |
| `src/editor/export/ExportModal.tsx` | Dimensions từ store, scrollStart type |
| `src/services/projectSerializer.ts` | Save/Open .prosvg |
| `src/services/svgSerializer.ts` | (giữ nguyên) |
